pipeline { 
    agent any  
    stages { 
        stage('Build') { 
            steps { 
                tools { 
                    maven 'Maven Apache'
                }
            }
        }
    }
}
